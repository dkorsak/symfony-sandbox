<script type="text/javascript">
    requirejs.config({baseUrl: '{{ app.request.basePath }}/{{ assetsPath }}/js'});
    require(['app/default'], function () {
        {% if module %}require(['{{ module }}']);{% endif %}
    });
</script>
